import React from 'react';
import ReactDOM from 'react-dom';
import './styles.css';
import GistGlance from './components/GistGlance';

ReactDOM.render(
  <React.StrictMode>
    <GistGlance />
  </React.StrictMode>,
  document.getElementById('root')
);
